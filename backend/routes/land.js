// backend/routes/land.js
const express = require('express');
const router = express.Router();

// Placeholder route for land data
router.get('/', (req, res) => {
  res.send('Land data route');
});

module.exports = router;
